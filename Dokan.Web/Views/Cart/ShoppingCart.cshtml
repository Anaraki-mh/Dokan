@model Dokan.Web.Models.CartModel

@{
    ViewBag.Title = "ShoppingCart";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="breacrumb-section">
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <div class="breadcrumb-text product-more">
                    <a href="/"><i class="fa fa-home"></i> Home</a>
                    <span>Cart</span>
                </div>
            </div>
        </div>
    </div>
</div>


<section class="shopping-cart spad">
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <div class="cart-table">
                    <table>
                        <thead>
                            <tr>
                                <th>Image</th>
                                <th class="p-name">Product Name</th>
                                <th>Price</th>
                                <th>Quantity</th>
                                <th>Total</th>
                                <th><i class="ti-close"></i></th>
                            </tr>
                        </thead>
                        <tbody>

                            @foreach (var item in Model.CartItems)
                            {

                                <tr id="cartItem-@item.ProductId">
                                    <td class="cart-pic first-row"><img src="~/Files/@item.ProductImage" alt="@item.ProductTitle"></td>
                                    <td class="cart-title first-row">
                                        <h5>
                                            @item.ProductTitle
                                        </h5>
                                    </td>
                                    <td class="p-price first-row">
                                        $@item.Price
                                    </td>
                                    <td class="qua-col first-row">
                                        <div class="quantity" onclick="UpdateQuantity(@item.ProductId)">
                                            <div class="pro-qty">

                                                <input id="p-@item.ProductId" type="text" value="@item.Quantity">

                                            </div>
                                        </div>
                                    </td>
                                    <td class="total-price first-row">$@Model.CartItems.Sum(x => x.Total).ToString("N0")</td>
                                    <td onclick="RemoveFromCart(@item.ProductId)" class="close-td first-row"><i class="ti-close"></i></td>
                                </tr>

                            }

                        </tbody>
                    </table>
                </div>

                <div class="row">
                    <div class="col-lg-4">
                        <div class="cart-buttons">
                            <a href="~/Products/Index" class="primary-btn continue-shop">Continue shopping</a>
                            <a onclick="location.reload()" class="primary-btn up-cart cursor-pointer">Update cart</a>
                        </div>
                        <div class="discount-coupon">
                            <h6>Discount Code</h6>
                            <form class="coupon-form">
                                @if (Model.CouponId == 0 || Model.CouponId is null)
                                {
                                    <input id="couponInput" type="text" placeholder="Enter your discount code">
                                    <button onclick="ApplyCoupon()" type="button" class="site-btn coupon-btn">Apply</button>
                                }
                                else
                                {
                                    <input disabled type="text" value="Coupon applied successfully!">
                                    <button disabled type="button" class="site-btn coupon-btn">Apply</button>
                                }
                            </form>
                        </div>
                    </div>

                    <div class="col-lg-4 offset-lg-4">
                        <div class="proceed-checkout">
                            <ul>
                                <li class="cart-total">Total <span>$@Model.CartItems.Sum(x => x.Total).ToString("N0")</span></li>
                            </ul>
                            <a @( Model.CartItems.Count > 0 ? Html.Raw("href=\"/Cart/Checkout\"") : Html.Raw("onclick='toastr.error(\"Cart is empty\")' href=\"#\"") ) class="proceed-btn">PROCEED TO CHECK OUT</a>
                        </div>
                    </div>
                </div>

            </div>
        </div>
    </div>
</section>
<script src="~/Scripts/jquery-3.4.1.min.js"></script>
<script src="~/Scripts/toastr.min.js"></script>
<script>

    function UpdateQuantity(productId) {
        var quantityInput = document.getElementById("p-" + productId);
        quantityInput.value = quantityInput.value.replace(/[^0-9]/g, '');
        var number = quantityInput.value;

        if (number == 0) {
            console.log("removed")
            RemoveFromCart(productId);
            return;
        }

        $.ajax({
            type: "GET",
            url: "/Cart/Add",
            dataType: "json",
            data: "productId=" + productId + "&number=" + number,
            success: function (data) {

            }
        });
        toastr.success('Updated quantity');
    }

    function RemoveFromCart(productId) {
        $.ajax({
            type: "GET",
            url: "/Cart/Add",
            dataType: "json",
            data: "productId=" + productId + "&number=0",
            success: function (data) {
                document.getElementById("cartItem-" + productId).remove();
            }
        });
        toastr.success('Removed from cart');
        document.getElementById("cartItem-" + productId).remove();
    }

    function ApplyCoupon() {

        $.ajax({
            type: "GET",
            url: "/Cart/ApplyCoupon",
            dataType: "json",
            data: "couponCode=" + document.getElementById("couponInput").value,
            success: function () {
                toastr.success('Coupon apllied successfully!');
                document.getElementById("couponInput").disabled = true;
                setTimeout(function () {
                    location.reload();
                }, 2000);
            },
            error: function () {
                toastr.error("Coupon doesn't exist or has been expired");
            }
        });
    }

</script>